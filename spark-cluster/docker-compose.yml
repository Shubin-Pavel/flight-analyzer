version: "3.8"
services:
  spark-master:
    image: spark-master:latest
    ports:
      - "4040:4040"
      - "7077:7077"
      - "18080:8080"
    volumes:
       - /mnt/c/main/SparkCourse/flight-analyzer/spark-cluster/spark-apps:/opt/spark-apps
       - /mnt/c/main/SparkCourse/flight-analyzer/spark-cluster/spark-data:/opt/spark-data
    environment:
      - "SPARK_LOCAL_IP=spark-master"
  spark-worker:
    image: spark-worker:latest
    depends_on:
      - spark-master
    environment:
      - SPARK_WORKER_CORES=1
      - SPARK_WORKER_MEMORY=1G
      - SPARK_DRIVER_MEMORY=528m
      - SPARK_EXECUTOR_MEMORY=528m
      - SPARK_MASTER=spark://spark-master:7077
    volumes:
       - /mnt/c/main/SparkCourse/flight-analyzer/spark-cluster/spark-apps:/opt/spark-apps
       - /mnt/c/main/SparkCourse/flight-analyzer/spark-cluster/spark-data:/opt/spark-data
